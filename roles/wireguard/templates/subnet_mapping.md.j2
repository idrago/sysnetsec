# roles/wireguard/templates/subnet_mapping.md.j2
# VM Subnet Mapping

This file shows which VM handles which portion of the {{ ctf_environment.wireguard.student_visible_network }} network for each student.

{% for student_id, student_subnets in subnet_mapping.items() | sort %}
## {{ student_id }}
{% for vm_name, vm_data in student_subnets.items() | sort %}
- {{ vm_name }}: {{ vm_data.subnet }} → {{ vm_data.vm_ip }}
{% endfor %}

{% endfor %}