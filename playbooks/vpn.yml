# playbooks/vpn.yml
---
- name: Configure WireGuard VPN
  hosts: localhost
  become: yes
  gather_facts: yes
  
  tasks:
    - name: Include WireGuard role
      include_role:
        name: wireguard
      tags: [vpn, setup]